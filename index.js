window.addEventListener("load", () => {
  // Random lowercase letter
  var lower = ['a','b'];

var randomItem = lower[Math.floor(Math.random()*myArray.length)];
  function randomLower() {
    
  }
});
